# CodeMentor-Agent
CodeMentor Agent helps instructors assess programming assignments consistently. It keeps an LLM-only baseline separate from a tool-using agent, then compares both with explicit, human-authored benchmark ground truth.
